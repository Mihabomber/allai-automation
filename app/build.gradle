plugins {
  id("com.android.application")
  id("org.jetbrains.kotlin.android")
}
android {
    namespace = "com.allai.automation"
    compileSdk = 34
    defaultConfig {
        applicationId = "com.allai.automation"
        minSdk = 26
        targetSdk = 34
        versionCode = 15
        versionName = "2.3"
    }
    signingConfigs {
        create("allai") {
            storeFile = rootProject.file("allai.keystore")
            storePassword = "allai2026"
            keyAlias = "allai"
            keyPassword = "allai2026"
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    buildTypes {
        debug { signingConfig = signingConfigs.getByName("allai") }
        release {
            minifyEnabled = false
            signingConfig = signingConfigs.getByName("allai")
        }
    }
    kotlinOptions { jvmTarget = "17" }
}
dependencies {
  implementation("com.google.mlkit:text-recognition:16.0.1")
  implementation("com.squareup.okhttp3:okhttp:4.12.0")
}
